// Fill these in once you've created a Supabase project (see README's
// "Cross-device sync" section for the exact steps). Both values are safe
// to commit — the anon key is meant to be public/embedded client-side;
// Row Level Security on each table is what actually protects your data,
// not the secrecy of this key.
window.SUPABASE_CONFIG = {
  url: '',      // e.g. https://xxxxxxxx.supabase.co
  anonKey: '',  // the "anon public" key from Project Settings -> API
};
